en:
  trace:

    bindings::binding/anchor_resolution:
      resolutions::space/by: |
        An anchor resolution for %{descriptor} is yet to be saved.
        Saving now.

    blueprints::space/import:
      git::space/import: |
        An attempt to import blueprint for '%{descriptor}' finds it already exists.
        Moving on.
      spaces::thing/open_struct_from_json:
        The blueprint for '%{descriptor}' is not well formed. Results are unpredictable.
      blueprints::space/import_by_json:
        The blueprint for '%{descriptor}' is not well formed. Results are unpredictable.

    docker::files::steps/rescue_in_step_for:
      docker::files::steps/general_step_for:
        Unable to find class %{name}.

    packing::space/block_in_execute_on_anchors_for:
      packing::space/by:
        A %{klass} for %{descriptor} is yet to be saved.
        Saving now.

    releases::division/new:
      releases::division/initialize: |
        Malformed %{klass} declaration in %{blueprint} blueprint.
        Ignoring %{content}

    releases::division/check_subdivision_class:
      releases::division/subdivision_class: |
        There is no source code for subdivision class %{klass}.
        Substituting OpenStruct.

    releases::division/subdivision_class:
      releases::division/const_get: |
        There is no source code for subdivision class %{klass}.
        Substituting OpenStruct.

    spaces::model/schema_class:
      spaces::model/require_relative:
        A schema for %{namespace} could not be found. Substituting %{subsitution}.

    spaces::space/open:
      spaces::space/initialize:
        An instance of %{klass} for %{descriptor} does not yet exist in its space.

    texts::text/map:
      texts::infix/resolved:
        Unable to resolve text infix for %{value} in %{text}.
