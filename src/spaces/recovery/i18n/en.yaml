en:
  trace:

    blueprinting::space/import:
      git::space/import: |
        An attempt to import blueprint for '%{descriptor}' finds it already exists.
        Moving on.
      spaces::thing/open_struct_from_json: |
        The blueprint for '%{descriptor}' is not well formed.
        Results are unpredictable.
      blueprinting::space/import_by_json: |
        The blueprint for '%{descriptor}' is not well formed.
        Results are unpredictable.

    packing::pack/auxiliary_texts:
      packing::pack/nominated_scripts: |
        There are no images given in the blueprint for '%{descriptor}'.
        So there's nothing to pack.

    packing::space/block_in_by:
      packing::space/save: |
        There are no images given in the blueprint for '%{descriptor}'.
        So there's no worthwhile pack to save.

    packing::space/block_in_execute_on_anchors_for:
      packing::space/by: |
        A %{klass} for %{descriptor} is yet to be saved.
        Saving now.

    packing::space/block_(2_levels)_in_execute_on_anchors_for:
      packing::space/execute: |
        There are no images given in the blueprint for '%{descriptor}'.
        So there's no worthwhile pack to %{command}.
      packing::space/by: |
        A pack for %{descriptor} is yet to be saved.
        Saving now.

    provisioning::space/apply:
      provisioning::space/execute:
        Provisioning failed while attempting to execute %{command} for %{identifier}.

    provisioning::space/plan:
      provisioning::space/execute:
        Provisioning failed while attempting to execute %{command} for %{identifier}.

    emitting::descriptive_subdivision/resolution:
      resolving::space/by: |
        A resolution for %{descriptor} is yet to be saved.
        Saving now.

    emitting::divisible/new:
      emitting::divisible/initialize: |
        Malformed %{klass} declaration in %{blueprint} blueprint.
        Ignoring %{content}

    emitting::divisible/check_subdivision_class:
      emitting::divisible/subdivision_class: |
        There is no source code for subdivision class %{klass}.
        Substituting OpenStruct.

    emitting::divisible/subdivision_class:
      emitting::divisible/const_get: |
        There is no source code for subdivision class %{klass}.
        Substituting OpenStruct.

    emitting::stanzas/block_in_stanzas:
      emitting::stanzas/stanza_for:
        There is no source code for stanza %{name}.

    spaces::models::model/composition_class:
      spaces::models::model/require_relative:
        A composition for %{namespace} could not be found. Substituting %{substitution}.

    spaces::models::space/open:
      spaces::models::space/initialize:
        An instance of %{klass} for %{descriptor} does not yet exist in its space.

    interpolating::text/map:
      interpolating::infix/resolved:
        Unable to resolve text infix for %{value} in %{text}.
